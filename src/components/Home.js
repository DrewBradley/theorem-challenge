import React, { useState } from 'react';

const Home = () => {
  return (
    <section classNames="Home">
      <button>New Random</button>
      <button>New Email</button>
    </section>
  )
}

export default Home;